import type { CTAData } from "@/content/types";

export const ctaData: CTAData = {
  headline: "Build form experiences that convert",
  description:
    "Start free. No account required. Add logic, publish in seconds, and connect to any tool via webhooks.",
  primaryCta: "Create a free form",
  badge: "Free to use",
};
